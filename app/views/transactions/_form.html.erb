<%= form_for(Transaction.new) do |f| %>
  <% if Transaction.new.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(Transaction.new.errors.count, "error") %> prohibited this transaction from being saved:</h2>

      <ul>
      <% Transaction.new.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :amount %><br>
    <%= f.text_field :amount %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
